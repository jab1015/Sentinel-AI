<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Sentinel.App.OptionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Sentinel AI Options">

    <Window.SystemBackdrop><MicaBackdrop /></Window.SystemBackdrop>

    <Grid Background="#111827" Padding="28">
        <StackPanel Spacing="20" MaxWidth="620" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Image Source="Assets/Shield.png" Width="30" Height="30"/>
                <TextBlock Text="Sentinel AI Options" FontSize="26" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="#1F2937" CornerRadius="14" Padding="24">
                <StackPanel Spacing="14">
                    <TextBlock Text="Startup" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Sentinel is added to startup by default so protection begins when you sign in. You can turn this off and restore it later." Foreground="#D1D5DB" TextWrapping="Wrap"/>
                    <ToggleSwitch x:Name="StartWithWindowsToggle"
                                  Header="Start Sentinel when I sign in"
                                  Toggled="StartWithWindowsToggle_Toggled"
                                  AutomationProperties.Name="Start Sentinel when I sign in"/>
                    <TextBlock x:Name="StartupStatusText" Foreground="#9CA3AF" TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Changes take effect immediately and are preserved during Sentinel updates." Foreground="#9CA3AF" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
